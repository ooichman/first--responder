apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-model-cache
  namespace: first-responder
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path  # Built-in default storage provisioner in K3s
  resources:
    requests:
      storage: 20Gi
